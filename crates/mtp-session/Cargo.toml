[package]
name = "mtp-session"
description = "One MTP session over two bulk endpoints. Command, data phase, response."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

# The crate reports what happened and writes nothing to the terminal. A
# filesystem daemon cannot print, and a probe wants to print its own words.
[dependencies]
ptp-proto = { path = "../ptp-proto" }
usb-freebsd = { path = "../usb-freebsd" }
